"use strict"; // treat all js code as newer version

// alert("Hello World") //we are using nodejs, not browser. so we'll get a ReferenceError: alert is not defined

let name = "chandini";
let isLoggedIn = false;
let state;

// Number
// BigInt
// String => we can use "" or '' (but "" is recommended)
// boolean => true/false
// null => standalone value
// undefined => means value is not assigned
// symbol => unique

// object

console.log(typeof undefined); //undefined
console.log(typeof null); //object
